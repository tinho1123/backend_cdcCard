{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:start": "npx sequelize db:create",
    "db:migration": "npx sequelize db:migrate",
    "db:drop": "npx sequelize db:drop",
    "db:seed": "npx sequelize db:seed:all",
    "start": "npm run db:drop && npm run db:start && npm run db:migration && npm run db:seed && node ./src/server.js",
    "dev": "npm run db:drop && npm run db:start && npm run db:migration && npm run db:seed && nodemon ./src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.21.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "sequelize-cli": "^6.4.1"
  }
}
